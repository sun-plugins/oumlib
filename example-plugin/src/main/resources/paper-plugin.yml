name: ExampleOumPlugin
version: 1.0.0
main: dev.oum.example.PaperExamplePlugin
api-version: '1.20'
folia-supported: true
description: An example plugin demonstrating the use of oumlib.
authors:
  - sun-dev

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
    MiniPlaceholders:
      load: BEFORE
      required: false
      join-classpath: true